<?xml version="1.0"?>
<ruleset name="FlossWare PMD Rules"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

    <description>FlossWare PMD Ruleset for code quality enforcement</description>

    <!-- Best Practices -->
    <rule ref="category/java/bestpractices.xml">
        <exclude name="JUnitAssertionsShouldIncludeMessage"/>
        <exclude name="JUnitTestContainsTooManyAsserts"/>
    </rule>

    <!-- System.out/err Usage Prevention - Critical for proper logging -->
    <rule name="NoSystemOutOrErr" language="java" message="Use proper logging instead of System.out.println/System.err.println" class="net.sourceforge.pmd.lang.rule.XPathRule">
        <description>
            Detects System.out.println(), System.out.print(), System.err.println(),
            and System.err.print() usage. These should be replaced with proper
            java.util.logging.Logger calls to enable production log management,
            rotation, and configuration.
        </description>
        <priority>3</priority>
        <properties>
            <property name="xpath">
                <value>
                    //PrimaryExpression[
                        PrimaryPrefix/Name[
                            contains(@Image, 'System.out') or
                            contains(@Image, 'System.err')
                        ]
                    ]
                    /PrimarySuffix[
                        MethodRef and (
                            @Image='println' or
                            @Image='print' or
                            @Image='printf'
                        )
                    ]
                </value>
            </property>
        </properties>
    </rule>

    <!-- Code Style -->
    <rule ref="category/java/codestyle.xml">
        <exclude name="AtLeastOneConstructor"/>
        <exclude name="CommentDefaultAccessModifier"/>
        <exclude name="OnlyOneReturn"/>
        <exclude name="LongVariable"/>
        <exclude name="ShortVariable"/>
        <exclude name="ShortClassName"/>
    </rule>

    <!-- Prefer method chaining - detect unnecessary local variables -->
    <rule ref="category/java/codestyle.xml/UnnecessaryLocalBeforeReturn"/>
    <rule ref="category/java/bestpractices.xml/UnusedLocalVariable"/>

    <!-- Design -->
    <rule ref="category/java/design.xml">
        <exclude name="LawOfDemeter"/>
        <exclude name="LoosePackageCoupling"/>
    </rule>
    <rule ref="category/java/design.xml/TooManyMethods">
        <properties>
            <property name="maxmethods" value="20"/>
        </properties>
    </rule>

    <!-- Documentation -->
    <rule ref="category/java/documentation.xml">
        <exclude name="CommentRequired"/>
        <exclude name="CommentSize"/>
    </rule>

    <!-- Error Prone -->
    <rule ref="category/java/errorprone.xml">
        <exclude name="BeanMembersShouldSerialize"/>
        <exclude name="DataflowAnomalyAnalysis"/>
    </rule>

    <!-- Multithreading -->
    <rule ref="category/java/multithreading.xml"/>

    <!-- Performance -->
    <rule ref="category/java/performance.xml"/>

    <!-- Security -->
    <rule ref="category/java/security.xml"/>
</ruleset>
